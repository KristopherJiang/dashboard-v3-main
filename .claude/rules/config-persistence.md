---
paths:
  - "**/*"
---

# 配置持久化规则

遇到以下信息时，**立刻**写入 `.claude/rules/dev-config.md`，不要等用户提醒：

- 数据库连接信息（URL、端口、用户名、密码）
- Web 管理工具的登录地址和账号
- Docker 容器的启动命令和配置
- 任何需要手动填写的配置步骤
- 环境变量和 .env 文件内容
- 第三方服务的 API endpoint 和 key 位置

**原则：** 新 session 必须能从 dev-config.md 中找到所有开发所需的配置信息，不需要用户重复说明。
